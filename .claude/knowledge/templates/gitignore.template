# Secrets (NEVER commit these)
.env
.env.local
.env.*.local
**/*.key
**/*.pem
**/credentials.json
**/secrets.json
**/secrets.yaml

# Local settings (machine-specific absolute paths - never commit)
.claude/settings.local.json
.claude/backups/
*.log

# NOTE: project-plan/ is deliberately NOT listed here.
# Your interview notes, design doc and improvement backlog are the record of
# why this workflow works the way it does. Keep them in git.

# Session data (workflow-specific)
# [WORKFLOW_SESSIONS_DIR]/

# Local MCP servers (dependencies and credentials)
mcp-servers/**/node_modules/
mcp-servers/**/.env
mcp-servers/**/*.log

# System files
.DS_Store
*.swp
*.swo
Thumbs.db

# Python (if applicable)
__pycache__/
*.py[cod]
*$py.class
.Python
venv/
env/
.venv

# Node (if applicable)
node_modules/
npm-debug.log
yarn-error.log

# Databases (if applicable)
*.db
*.sqlite
*.sqlite3
