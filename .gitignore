# Secrets (NEVER commit)
.env
.env.local
.env.*.local
**/*.key
**/*.pem
**/credentials.json
**/secrets.json
**/secrets.yaml

# Local settings with explicit paths - don't commit
.claude/settings.local.json

# NOTE: project-plan/ is deliberately NOT ignored.
# It holds your interview notes, design doc and improvement backlog - the
# record of why your workflow is built the way it is. That belongs in git.

# Internal documentation - keep local only
docs/internal/

# Dependencies
node_modules/
__pycache__/
*.pyc
*.py[cod]
.venv/
venv/

# System files
.DS_Store
*.swp
*.swo
Thumbs.db
